package com.example.demo.security;

/**
 * Enum representing user roles in the application.
 * Each role has a name that can be used for authorization checks.
 */

public enum Role {

    USER,
    ADMIN;


} 